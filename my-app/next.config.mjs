/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        'github.githubassets.com',    // GitHub favicon domain
        'static-exp1.licdn.com',      // LinkedIn favicon domain
        'www.instagram.com',          // Instagram favicon domain
      ],
    },
  }

export default nextConfig;
